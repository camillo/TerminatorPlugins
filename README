This are some plugins for terminator, I wrote for my own convenience.

see https://launchpad.net/terminator 
for information about terminator.

see ./doc folder for detailed docu about the plugins.

Following plugins are included, yet:
- LayoutManager
  Saves and restores Layouts, plus some extra functions for custom commands.

- TerminalExporter
  Exports full terminal (including buffer) into file.
  Allows to log terminal's action into file.

Code for all plugins is public domain.
